@model IEnumerable<WebApplication2.Models.DocumentConstructorCenterData>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Home/Index.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DocumentConstructorCenterDataId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DocumentConstructorCenterDataId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DocumentConstructorCenterDataId })
        </td>
    </tr>
}

</table>
